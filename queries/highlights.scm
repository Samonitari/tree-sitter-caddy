(snippet_name) @function

[
  "abort"
  "bind"
  "encode"
  "header"
  "redir"
  "tls"
] @keyword

[
  (matcher_name)
  (matcher_token)
] @tag

(header_name) @string.special
(header_value) @string

(comment) @comment

["{" "}"] @punctuation.bracket
